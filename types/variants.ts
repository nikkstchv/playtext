export interface variants {
  primary: string
  secondary: string
  third: string
}

export enum enumVariants {
  primary = 'primary',
  secondary = 'secondary',
  third = 'third',
}

export interface sizes {
  superSmall?: string
  custom?: string
  small: string
  medium: string
  large: string
}

export enum enumSizes {
  custom = 'custom',
  small = 'small',
  medium = 'medium',
  large = 'large',
}
